spring:
  application:
    name: dine-in-order

  datasource:
    url: jdbc:mysql://localhost:3306/din_in_order?createDatabaseIfNotExist=true
    username: root
    password: ilovebangalore

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
